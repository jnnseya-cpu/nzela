/*
 * Blog view-counter config. Point this at the deployed @nzela/views service.
 * Leave endpoint empty to keep the counter inert (nothing shows, no calls).
 *
 *   endpoint : base URL of the views service, e.g. "https://views.tunakula.com"
 *   enabled  : master switch
 */
window.NZELA_VIEWS_CONFIG = {
  endpoint: "",
  enabled: true,
};
