# Deep Audit — 20-day sweep (2026-08-19)

A full ground-truth review of the last ~20 days (commits `1aa8415` →
`2395a31`) to find and clear everything outstanding, unfinished, skipped, or
forgotten. Method: hard verification (tests/typecheck/git), a repo-wide
loose-end grep, and three parallel deep audits (backend/shared code ·
frontend + consistency · docs/decisions). This is the record so the same
items are not re-discovered next session.

## Verified ground truth (not assumed)

- **179 tests passing (26 files)**, `tsc --noEmit` clean, git tree clean.
- **Zero real orphan TODO/FIXME** in code — the only marker hits were a
  `TK-xxx` ref format, an intentional test stub, and documented Phase-2
  deferrals.
- The code that exists is real and tested. The "outstanding" work is **not
  rotting code** — it is status drift, unbuilt surfaces, injected ports
  awaiting real backends, and unclosed decisions.

---

## A. Cleared this session (code + docs) ✅

| # | Item | Fix |
|---|---|---|
| A-1 | Blog generator baked a stale placeholder number `wa.me/243000000000` (`frontend/blog/_generate.ts`) — re-running it would overwrite every article with the wrong number. | → `447493216101`. |
| A-2 | Current prototype half-migrated: 5× `drc.tunakula.com` mixed with `cd.` (`docs/prototype/…interactive.html`). | All → `cd.tunakula.com`. |
| A-3 | Orphan duplicate prototype `…interactive-v1.html` (referenced nowhere, stale copy). | Deleted. |
| A-4 | `PasswordVault` was the **only auth port with no implementation at all** (`shared/stackfood-client/src/auth.ts`) — StackFood auto-provisioning couldn't run end-to-end without an inline stub. | Added `InMemoryPasswordVault` + 6 tests. |
| A-5 | Referral-code alphabet contained stray Arabic characters sliced off by `.slice(0,10)` (`backend/acquisition/src/referral.ts`) — dead copy-paste residue. | Cleaned to `"ABCDEFGHJK"` (identical output, verified by the regex test). |
| A-6 | `backend/agents/src/index.ts` header claimed "deterministic fallbacks" live in this package — they live in the gateway. Misleading. | Comment corrected. |
| A-7 | `LIPA_BOX_BUILD.md` named the 4th operator "Afrimoney" — every other doc says "Africell Money" (ERRATA E-5). | → "Africell Money". |
| A-8 | `KODA_Integration_Notes.md` used the pre-E-5 path `apps/lipa-ingest`. | → `backend/lipa-ingest`. |
| A-9 | Stale test counts across docs: STATUS "163/24", GO_LIVE_READINESS "102+21", RUNBOOK "162", README "8-agent registry". | All reconciled to 179/26 and 11 agents. |
| A-10 | Agent-count drift (8 in handoff docs vs 11 shipped) never officially reconciled. | Added **ERRATA E-9** as the single reconciliation record. |
| A-11 | STATUS.md decisions were scattered; no single place listed the durability gaps. | Added a consolidated **Open decisions** table + a **biggest code gap** note + fuller persistence-port list. |

All A-items are committed and re-verified green.

---

## B. Needs the owner's decision (not code) — see STATUS.md "Open decisions"

- **D-1 — the customer-facing WhatsApp number (+44 vs a local +243).** The
  single most important open item: shipped assets carry +44, the runbooks
  carry a `243…` placeholder; they can't both be right. Nothing
  customer-facing can go live correctly until this is fixed.
- **D-2 — card vs the ×2 rule** (surcharge / diaspora-only / drop at launch).
- **D-3 — the four merchant SIM numbers** to print in pay instructions.
- **D-4 — KODA Master Spec v2.0** (commit it, or mark KODA out-of-scope).
- **D-5 — escalation calls** (TTS provider vs manual ops for the pilot).

---

## C. Human / operational — cannot be code-cleared (the real launch gate)

Unchanged and honest: the critical path to a first real customer is not
code. In order:

1. Live WhatsApp number + operator (manual channel) — `SATURDAY_LAUNCH_RUNBOOK`.
2. 3–5 signed Bandal restaurants + 5–8 wewas.
3. `cd.tunakula.com` service tokens + the `/api/v1/config` capture + one real
   test order.
4. One real mobile-money payment verified end-to-end.
5. Then deploy gateway/lipa; wire persistence (Redis/PG), LLM keys, the
   WhatsApp Cloud API adapter, and commission the Lipa Box hardware.

## Deferred code tiers (Phase 2 / at deploy — gated on C above + keys)

- **Gateway conversation/dispatch layer** — the one *missing layer* (order
  loop can't reply yet); deterministic build is possible without LLM keys
  but depends on D-1 + the Cloud API adapter. Highest-value next build.
- **`backend/agents`** — LangGraph LLM tier is interfaces-only by design.
- **No LLM client anywhere** — every model seam (seo `DraftWriter`, growth
  `Generator`, sentinelle `ThreatTriage`, commande/adresse) has a
  deterministic fallback; real prose needs API keys.
- **Production adapters** — `EmailSender`, `CreditIssuer`, `WhatsAppSender`,
  production `PasswordVault`/`TokenCache`/`ReplayIndex`/`RateLimiter`,
  schema migrations.

---

*Bottom line: the code bucket is now clean and honest — every stale count,
placeholder, and copy-paste defect found in the 20-day window is fixed or
explicitly recorded. What remains is decisions (B) and operational launch
steps (C), neither of which is code. STATUS.md is the live source of truth;
this file is the point-in-time audit record.*
